# /etc/secrets/wireless.env
# This file is NOT managed by Nix — create it manually on the machine
# It is NOT committed to git — add to .gitignore
#
# To create on the server after install:
#   sudo mkdir -p /etc/secrets
#   sudo chmod 700 /etc/secrets
#   sudo nano /etc/secrets/wireless.env
#   sudo chmod 600 /etc/secrets/wireless.env
#
# Contents of the file (replace with your actual password):
password=YOUR_WIFI_PASSWORD_HERE
